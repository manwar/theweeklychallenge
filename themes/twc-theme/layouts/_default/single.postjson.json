{{- $content := .RawContent -}}
{{- $content = $content | replaceRE `<br\s*/?>` "\n" -}}
{{- $content = $content | replaceRE `src="(/[^"]+)"` "src=\"https://theweeklychallenge.org$1\"" -}}
{{- $content = $content | replaceRE `(?m)[ \t]*<img\s[^>]*src="([^"]+)"[^>]*>` "\n![image]($1)\n" -}}
{{- $content = $content | replaceRE `<[^>]+>` "" -}}
{{- $content = $content | replaceRE `(?m)^[|\s]+$` "" -}}
{{- $content = $content | replaceRE ` \| ` " " -}}
{{- $content = $content | replaceRE `(?m)^\|` "" -}}
{{- $content = $content | replaceRE `(?m)\|$` "" -}}
{{- $content = $content | replaceRE `\|` "" -}}
{
  "slug":    {{ .File.BaseFileName | jsonify }},
  "title":   {{ .Title | jsonify }},
  "author":  {{ .Params.author | jsonify }},
  "date":    {{ .Date.Format "2 Jan 2006" | jsonify }},
  "image":   {{ with .Params.image }}{{ printf "https://theweeklychallenge.org/%s" . | jsonify }}{{ else }}""{{ end }},
  "content": {{ $content | jsonify }}
}
